[include]
  path = ~/.config/delta/themes.gitconfig

[user]
  name  = kfernandez31
  email = k66901783+kfernandez31@users.noreply.github.com

[core]
  untrackedCache = true
  autocrlf       = input
  editor         = $EDITOR
  pager          = delta \
    --pager='less --mouse' 
    ; --diff-so-fancy --line-numbers

[delta]
  features = fzf line-numbers decorations diff-so-fancy
  ; syntax-theme                = "Catppuccin Mocha"
  navigate                    = true
  side-by-side                = false
  true-color                  = always
  hyperlinks                  = true
  hyperlinks-file-link-format = "vscode://file/{path}:{line}" # TODO: configure for nvim
  ; minus-style = red bold ul "#ffeeee"
  ; minus-style = normal bold normal 

[color]
  ui = auto

[interactive]
  diffFilter = delta --color-only

[init]
  defaultBranch = main

[merge]
  conflictstyle = diff3

[push]
  autoSetupRemote = true

[pull]
  rebase = true

[diff]
  colorMoved = default

[stash]
	showDiff = true
	showPatch = true

[help]
  autocorrect = 1

[alias]
  # log
  l    = log
  lol  = log --graph --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset'
  lola = log --graph --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --all
  lols = log --graph --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%ar) %C(bold blue)<%an>%Creset' --stat
  # add
  a   = add
  aa  = add --all
  ap  = add --patch
  au  = add --update
  aap = add --all --patch
  aau = add --all --update
  # branch
  b   = branch
  ba  = branch --all
  bd  = branch --delete
  bdf = branch --delete --force
  bm  = branch --move
  bn  = branch --no-merged
  br  = branch --remote
  # checkout
  co = checkout
  cb = checkout -b
  # diff
  d  = diff
  ds = diff --staged
  dH = diff HEAD
  # cherry-pick
  cp  = cherry-pick
  cpa = cherry-pick --abort
  cpc = cherry-pick --continue
  # clean
  cl  = clean
  clx = clean -x
  # commit
  c     = commit --verbose
  ca    = commit --verbose --all
  cm    = commit --verbose --message
  cam   = commit --verbose --all --message
  cane  = commit --verbose --amend --no-edit
  caane = commit --verbose --amend --all --no-edit
  # fetch
  f = fetch
  # init
  i = init
  # merge
  m  = merge
  ma = merge --abort
  mc = merge --continue
  # pull
  pl   = pull
  plv  = pull --verbose 
  plr  = pull --rebase --verbose
  plra = pull --rebase --verbose --autostash
  # push
  ps     = push
  psd    = push --dry-run
  psf    = push --force-with-lease --force-if-includes
  pssupo = push --set-upstream origin $(git_current_branch)
  psod   = push origin --delete
  # rebase
  rb  = rebase
  rba = rebase --abort
  rbc = rebase --continue
  rbi = rebase --interactive
  rbs = rebase --skip
  rbm = rebase $(git_main_branch)
  # remote
  rv   = remote --verbose
  ra   = remote add
  rrm  = remote remove
  rmv  = remote rename
  rset = remote set-url
  rup  = remote update
  # reset
  res  = reset
  resH = reset HEAD
  # restore
  rs = restore --staged
  # rm
  rmc = rm --cached -rf
  # show
  sh = show --pretty=short --show-signature
  # status
  st  = status
  sts = status --short --branch
  # submodule
  si = submodule init
  su = submodule update
  # stash
  sta  = stash apply
  stc  = stash clear
  std  = stash drop
  stp  = stash push
  stpa = stash push --all
  stpu = stash push --include-untracked
  stsp = stash show --patch
  stl  = stash list
  # switch
  sw = switch

  # extras!

  # search
  se = !git rev-list --all | xargs git grep -F
